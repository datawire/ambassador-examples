---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ build.name }}
  name: {{ build.name }}
  namespace: {{ service.namespace }}
spec:
  type: NodePort
  ports:
    - port: 80
      nodePort: 30900
      targetPort: envoy
  selector:
    app: {{ build.name }}
